export interface IOnBack {
  location: string,
  is_search: boolean
}
export interface IOnNext {
  // location: string,
  // is_search: boolean
}
export interface IOnClick {
  location: string,
  is_search: boolean
}
export interface IOnLocation {
  location: string,
  is_search: boolean
}